import type { IInquiryInput } from "@/types/inquiry"
import { brandEmail, adminEmailUrl } from "./brand-template"
export const getAdminInquiryEmailTemplate = (data: IInquiryInput) => brandEmail({ label: "NEW CUSTOMER ENQUIRY", title: "A customer would like your help.", intro: "Review the enquiry and guide the customer towards a suitable solution.", fields: [["Customer", data.name], ["Phone", data.phone], ["Email", data.email], ["Subject", data.subject], ["Requirement", data.message]], action: "Review CRM Leads", href: adminEmailUrl("/admin/leads"), nextStep: "Find the customer in your CRM, review the requirement and arrange the next follow-up." }).html
